---
import Layout from "@layouts/Layout.astro";
const { name } = Astro.params;

const example = await new Promise((resolve) => setTimeout(() => resolve(Math.random()), 500));
---

<Layout title="Welcome to Astro.">
  <h1>{name}</h1>
  <div>
    {example}
  </div>
</Layout>
