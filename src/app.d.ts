// for information about these interfaces
declare global {
  namespace App {
    // interface Error {}
    interface Locals {
      userId?: string;
    }
    // interface PageData {}
    // interface Platform {}
  }
}

export {};
